{% extends "base.twig" %}

{% block content %}
    <main>
        <h1>{{ title }}</h1>
        <p>{{ category }}</p>
        {# <p>{{ domain_technician }}</p>
        <p>{{ domain_organization }}</p>
        <p>{{ style }}</p>
        <p>{{ instrument }}</p>
        <p>{{ type }}</p> #}
        <p>{{ name }}</p>
        <p>{{ first_name }}</p>
        <p>{{ creation_year }}</p>
        <a href="mailto:{{ e-mail }}">{{ e-mail }}</a> </br>
        <a href="tel:+{{ phone_number }}">{{ phone_number }}</a> </br>
        <p>{{ district }}</p>
        <p>{{ years_of_experience }}</p>
        <img src="{{ profile_picture.url }}" />
        <p>{{ description }}</p>
        <a href="{{ website }}">{{ website }}</a> </br>
        <a href="{{ video_1 }}">{{ video_1 }}</a> </br>
        <a href="{{ video_2 }}">{{ video_2 }}</a> </br>
        <a href="{{ video_3 }}">{{ video_3 }}</a> </br>
        <a href="{{ instagram }}">{{ instagram }}</a> </br>
        <a href="{{ youtube }}">{{ youtube }}</a> </br>
        <a href="{{ tiktok }}">{{ tiktok }}</a> </br>
        <a href="{{ facebook }}">{{ facebook }}</a>
    </main>
{% endblock %}
