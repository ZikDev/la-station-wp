{% extends "base.twig" %}

{% block content %}
    <main>
        <h1>{{ title }}</h1>
        <h2>{{ catch_phrase }}</h2>
        <p>{{ introduction }}</p>

        {% include 'components/form.twig' with {districts:districts} %}
    </main>
{% endblock %}
